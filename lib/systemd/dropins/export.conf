[Service]

# CERTHUB_RSYNC_ARGS
# Arguments for rsync.
#
Environment="CERTHUB_RSYNC_ARGS=\
    --checksum \
    --delete \
    --devices \
    --links \
    --perms \
    --recursive \
    --specials \
    --verbose"
